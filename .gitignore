# เอกสารกลยุทธ์ชุดใหญ่ (index.html) ไม่อยู่ใน repo นี้โดยตั้งใจ
# repo นี้เก็บเฉพาะหน้า Digital Organization
index.html
assets/img0*

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node (tooling only — no build step)
node_modules/
npm-debug.log*

# Scratch
*.b64
