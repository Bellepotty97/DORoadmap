# เอกสารกลยุทธ์ชุดใหญ่ (PCC 4-Phase) ไม่อยู่ใน repo นี้
# repo นี้เก็บเฉพาะหน้า Digital Organization
assets/img0*

# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node (tooling only — no build step)
node_modules/
npm-debug.log*

# Scratch
*.b64
